@page "/claims"

<AuthorizeView>
    <Authorized>
        <ul>
            @foreach (var claim in context.User.Claims)
            {
            <li> <b>@claim.Type : @claim.Value</b></li>
            }
        </ul>
    </Authorized>
</AuthorizeView>

