@page "/UserName"

<AuthorizeView>
    <Authorized>

        @foreach (var claim in context.User.Claims)
        {
            if (claim.Type == "name")
            {
                <span style="margin: 5px;">@claim.Value</span>
            }
        }

    </Authorized>
</AuthorizeView>